let numbers = [2, 3, 4, 5, 6];
let squersNumbers = numbers.map((numbers) => numbers * numbers);
console.log(squersNumbers);

//let squareNumbers = numbers.map(function (num) {
// return num ** 2;
//  });

let cars = [ "BMW", "Mercedes","Audi", "Opel"]

//let newCars = cars.map(car=>car.toUpperCase());
// console.log(newCars);

let search = "e";

